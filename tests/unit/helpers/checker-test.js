
import { checker } from 'social-web-infinity/helpers/checker';
import { module, test } from 'qunit';

module('Unit | Helper | checker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checker([42]);
  assert.ok(result);
});

