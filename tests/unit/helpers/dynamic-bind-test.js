
import { dynamicBind } from 'social-web-infinity/helpers/dynamic-bind';
import { module, test } from 'qunit';

module('Unit | Helper | dynamic bind');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dynamicBind([42]);
  assert.ok(result);
});

